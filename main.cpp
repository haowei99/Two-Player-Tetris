#include "block.h"
#include "board.h"
#include "iBlock.h"
#include <iostream>
using namespace std;


int main() {
/*
    vector<vector<int>> new_grid(15, vector<Cell>(11));
    Board * board = nullptr;
    iBlock b = iBlock(0, 0, 1, board);
    b.rotateClockwise();

    */

}

