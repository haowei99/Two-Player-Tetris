#include "block.h"
#include "board.h"
#include "oBlock.h"
#include <iostream>
#include <string>
#include "game.h"
using namespace std;


int main() {
    Board b(0, 0);
    cout << "Board:" << endl;
    char c;
    oBlock ob(0, 0, 0 b);
    while (cin >> c){
        if (c == 'r'){ // right
        }
        else if (c == 'l'){ //left
            
        }
        else if (c == 'd'){ //down 
            
        }
    }
/*
    vector<vector<int>> new_grid(15, vector<Cell>(11));
    Board * board = nullptr;
    iBlock b = iBlock(0, 0, 1, board);
    b.rotateClockwise();

    */
cout << "Hello World" << endl;

}

